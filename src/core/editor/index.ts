/**
 * Editor Module
 * Exports the EditorController (internal use only).
 */

export { EditorController } from './editor-controller';
export type { 
  DispatchFn, 
  TransactionListener, 
  EditorControllerConfig 
} from './editor-controller.types';
